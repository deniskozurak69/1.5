@model List<Article>

<h2>Articles in @ViewBag.CategoryName</h2>

@if (Model.Any())
{
        <table class="table">
            <thead>
                <tr>
                    <th>Article ID</th>
                    <th>Article Name</th>
                    <th>Author</th>
                        <th>Publish Date</th>
                    
                </tr>
            </thead>
            <tbody>
            @foreach (var article in Model)
            {
                        <tr>
                            <td>@article.ArticleId</td>
                            <td>@article.ArticleName</td>
                            <td>@article.AuthorUsername</td>
                             <td>@article.PublishDate</td>
                        </tr>
            }
            </tbody>
        </table>
}
else
{
        <p>No articles found in this category.</p>
}

<a asp-controller="Categories" asp-action="Index">Back to Categories</a>